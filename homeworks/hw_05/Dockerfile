# Лёгкая база
FROM node:22-alpine

# Рабочая директория внутри контейнера
WORKDIR /hw_05-app

# Копируем только то, что нужно
COPY . .

# Запускаемся не от root — хорошая практика безопасности
USER node

# Команда по умолчанию
CMD ["node", "script.js"]
